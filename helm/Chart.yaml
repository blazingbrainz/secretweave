apiVersion: v2
name: secretweave
description: Syncs annotated Kubernetes Secrets from a parent namespace to target namespaces; reacts immediately to new namespace creation.
type: application
version: 0.1.0
appVersion: "0.1.0"
