{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "secretweave.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "secretweave.labels" . | nindent 4 }}
{{- end }}
